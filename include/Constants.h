#pragma once
#include <algorithm>
#include <iostream>
#include <vector>
#include <memory>
#include <string>

const uint16_t LEAST_THROW_VALUE = 0;
const uint16_t MAX_THROW_VALUE = 10;
const uint16_t MAX_SCORE = 300 ;
const uint16_t MAX_NUMBER_OF_FRAME = 10;
const uint16_t THROW_ONE = 1;
const uint16_t THROW_SECOND = 2;
const uint16_t THROW_THREE = 3;
